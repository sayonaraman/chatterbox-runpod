# Core Chatterbox TTS dependencies (GPU optimized)
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.30.0
accelerate>=0.20.0
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0
scipy>=1.10.0
einops>=0.7.0
omegaconf>=2.3.0
hydra-core>=1.3.0

# RunPod Serverless API (minimal)
runpod>=1.5.0

# Audio processing (essential only)
ffmpeg-python>=0.2.0

# Minimal utilities
requests>=2.31.0
tqdm>=4.65.0

# Watermarking (optional, may fail gracefully)
perth>=0.1.0
