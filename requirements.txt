# Core Chatterbox TTS dependencies (GPU optimized, fixed versions)
torch==2.1.0
torchaudio==2.1.0
transformers==4.35.2
accelerate==0.24.1
librosa==0.10.1
soundfile==0.12.1
numpy==1.24.3
scipy==1.11.4
einops==0.7.0
omegaconf==2.3.0
hydra-core==1.3.2

# RunPod Serverless API (minimal)
runpod>=1.5.0

# Audio processing (essential only)
ffmpeg-python==0.2.0

# Minimal utilities
requests==2.31.0
tqdm==4.66.1

# Watermarking (optional, may fail gracefully)
perth>=0.1.0
