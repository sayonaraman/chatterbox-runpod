# Core Chatterbox TTS dependencies (from official pyproject.toml)
numpy>=1.24.0,<1.26.0
librosa==0.11.0
s3tokenizer
torch==2.6.0
torchaudio==2.6.0
transformers==4.46.3
diffusers==0.29.0
resemble-perth==1.0.1
conformer==0.3.2
safetensors==0.5.3
pkuseg==0.0.25
pykakasi==2.3.0

# RunPod Serverless API (minimal)
runpod>=1.5.0

# Audio processing (essential only)
ffmpeg-python>=0.2.0

# Minimal utilities
requests>=2.31.0
tqdm>=4.65.0
